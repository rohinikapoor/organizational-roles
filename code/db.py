import csv
import MySQLdb


def get_emails(num_emails):
    """This should either load from a previously saved dump,
    or create a new dump and return a list of emails containing
    sender, recipient(s) and body"""
    pass


def dump_csv(num_emails):
    """Helper function so that clients without a db setup can quickly query for emails
    and receive the required data from a csv file. This file should be pushed onto GitHub"""
    pass
